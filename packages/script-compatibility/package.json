{
  "name": "@clocktower-nexus/script-compatibility",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Script compatibility helper workspace.",
  "scripts": {
    "build": "node -e \"console.log('Script compatibility package build wiring will be added once package outputs are defined.')\"",
    "test": "node -e \"console.log('Script compatibility package tests will be added once compatibility behavior exists.')\"",
    "lint": "node -e \"console.log('Script compatibility package lint wiring will be added in Task 1.1.5.')\"",
    "typecheck": "tsc -p tsconfig.json"
  }
}
