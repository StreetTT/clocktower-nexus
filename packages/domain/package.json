{
  "name": "@clocktower-nexus/domain",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Canonical game state and domain logic workspace.",
  "exports": {
    ".": "./index.ts",
    "./state": "./state.ts",
    "./commands": "./commands.ts",
    "./events": "./events.ts",
    "./reducers": "./reducers.ts",
    "./projectors": "./projectors.ts"
  },
  "scripts": {
    "build": "node -e \"console.log('Domain package build wiring will be added once package outputs are defined.')\"",
    "test": "vitest run",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json"
  }
}
