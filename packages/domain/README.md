# Domain

This workspace will hold the canonical game state, semantic commands, events, reducers, and projection logic.

It is the shared core of the system and should keep gameplay rules and state transitions outside the UI.

This is a placeholder workspace for Task 1.1.2. Package internals arrive in later tasks.
