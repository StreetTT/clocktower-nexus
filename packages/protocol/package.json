{
  "name": "@clocktower-nexus/protocol",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Shared transport contract workspace.",
  "scripts": {
    "build": "node -e \"console.log('Protocol package build wiring will be added once package outputs are defined.')\"",
    "test": "node -e \"console.log('Protocol package tests will be added once shared contracts exist.')\"",
    "lint": "node -e \"console.log('Protocol package lint wiring will be added in Task 1.1.5.')\"",
    "typecheck": "tsc -p tsconfig.json"
  }
}
