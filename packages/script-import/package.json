{
  "name": "@clocktower-nexus/script-import",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Script import helper workspace.",
  "scripts": {
    "build": "node -e \"console.log('Script import package build wiring will be added once package outputs are defined.')\"",
    "test": "node -e \"console.log('Script import package tests will be added once import behavior exists.')\"",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json"
  }
}
