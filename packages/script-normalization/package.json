{
  "name": "@clocktower-nexus/script-normalization",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Script normalization helper workspace.",
  "scripts": {
    "build": "node -e \"console.log('Script normalization package build wiring will be added once package outputs are defined.')\"",
    "test": "node -e \"console.log('Script normalization package tests will be added once normalization behavior exists.')\"",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json"
  }
}
