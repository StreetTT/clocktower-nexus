# Script Normalization

This workspace will hold helpers for converting imported script data into a stable internal representation.

It should isolate normalized script modeling from raw import concerns and compatibility adapters.

This is a placeholder workspace for Task 1.1.2. Package internals arrive in later tasks.
