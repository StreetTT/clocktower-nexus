{
  "name": "@clocktower-nexus/game-hub",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Server-authoritative game hub backend workspace.",
  "dependencies": {
    "@clocktower-nexus/domain": "*",
    "@clocktower-nexus/protocol": "*",
    "fastify": "^5.0.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0"
  },
  "scripts": {
    "dev": "node --watch --import tsx src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json"
  }
}
